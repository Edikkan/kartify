apiVersion: v1
kind: Service
metadata:
  name: kartify-frontend
  namespace: kartify
  labels:
    app: kartify-frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: kartify-frontend

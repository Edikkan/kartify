apiVersion: v1
kind: Service
metadata:
  name: kartify-redis
  namespace: kartify
  labels:
    app: kartify-redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: kartify-redis

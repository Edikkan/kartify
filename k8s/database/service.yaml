apiVersion: v1
kind: Service
metadata:
  name: kartify-postgres
  namespace: kartify
  labels:
    app: kartify-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: kartify-postgres
